package bg.documentor.enums;

/**
 * The Validators enumeration.
 */
public enum Validator {

	min, max, required, requiredTrue, email, minLength, maxLength, pattern /** used to apply regex expression **/
}
